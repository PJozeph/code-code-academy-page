import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'code-code-academy-page',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
